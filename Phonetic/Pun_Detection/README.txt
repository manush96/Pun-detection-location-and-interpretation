Input - File Containing Contexts
Output - 1 (contains a pun) 
		or
0 (does not contain a pun) for each context.

The output file contain a dictionary in which each pun is given a ID "het_id" where id is a number and the value of key is 0 or 1 correspondingly.

